# Controller Interface

_works only on linux_

This program lets you use any device as a micro controller.
The program will listen to events and do stuff.
The 'micro controller's' events will be stolen from the OS.

device.conf contains the path of the input device that will be used.
events.conf contains the events based on the key codes.
events.conf must end with an empty line.